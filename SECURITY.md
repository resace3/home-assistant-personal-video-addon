# Security policy

Report vulnerabilities through GitHub Security Advisories, never a public issue containing private media, filenames, Home Assistant URLs, cookies, user identities, logs, or credentials.

The viewer is admin-only, Ingress-only, read-only toward shared media, and intentionally has no Supervisor/Home Assistant API privileges. Public CI uses synthetic content. Before publication, scan the full history and working tree with Gitleaks.

